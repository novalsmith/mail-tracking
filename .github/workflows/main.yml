name: Workflow CI/CD

# Control when the action will run

on:
  push: 
     branches: 
        - main
  
  # Allow you to run this workflow manualy from the actions tab
  workflow_dispatch:

jobs:
  build:
    name: Build ⛏
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@main  
  Dev:
    runs-on: ubuntu-latest
    environment: Dev
    steps: 
      - name: Checkout repository
        uses: actions/checkout@main

      # - name: Sync files to service
      #   uses: SamKirkland/FTP-Deploy-Action@4.3.1
      #   with:
      #     server: service.dev.mosframtech.com
      #     username: dev_core@dev.mail-tracker-minerba.mosframtech.com
      #     password: dev_core
      #     local-dir: starter/
      #     exclude: |
      #       .git/**
      #       .git**
      #       .git*/**
      #       **/.git*
      #       **/.git*/**
      #       **/node_modules/**
      #       fileToExclude.txt
      #       **/dist/index.js/**
      #       **/tests/**
      #       **/public/**
      #       **/src/**
      #       **/service/**

      # - name: Sync files to publics
      #   uses: SamKirkland/FTP-Deploy-Action@4.3.1
      #   with:
      #     server: service.dev.mosframtech.com
      #     username: dev_public@service.dev.mosframtech.com
      #     password: dev_public
      #     local-dir: starter/public/
      #     exclude: |
      #       .git/**
      #       .git**
      #       .git*/**
      #       **/.git*
      #       **/.git*/**
      #       **/node_modules/**
      #       fileToExclude.txt
      #       **/dist/index.js/**
      #       **/src/**

      - name: Use Node.js 16
        uses: actions/setup-node@main
        with:
          node-version: '16'

      - name: 🔨 Build Project
        run: |
          npm install
          npm run build

      - name: Sync files to view
        uses: SamKirkland/FTP-Deploy-Action@4.3.2
        with:
          server: dev.mail-tracker-minerba.mosframtech.com
          username: ftp-dev-mail-tracking-minerba@dev.mail-tracker-minerba.mosframtech.com
          password: ftp-dev-mail-tracking-minerba
          local-dir: dist/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/public/**
            **/src/**
            **/service/**
            **/starter/**
            